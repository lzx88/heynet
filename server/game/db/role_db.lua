local role = {}

function role.load(DB, KEY)
	local data = {
		id = 1,
		name = "heynet",
		level = 5,
	}

	return data
end

function role.isexists(DB, KEY, name)
	return data
end

function role.create(DB, KEY, name, userid)
	local roleid = 1
	return roleid
end

return role